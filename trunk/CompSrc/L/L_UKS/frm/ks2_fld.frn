.fields
!Шапка
  strHeader
  #OrgOnlyNoUNN (Инвестор)
  Инвестор_ОКПО
  #OrgOnlyNoUNN (Заказчик)
  Заказчик_ОКПО
  #OrgOnlyNoUNN (Подрядчик)
  Подрядчик_ОКПО
  Стройка + if (Стройка_Адрес <> '', ' Адрес: ' + Стройка_Адрес, '')
  Объект  + if (Trim(Объект_Аббр) = '', '', ', инв.№' + Объект_Аббр)
  Заказчик_ОКДП
  Договор_Номер

  if (Договор_Дата = Date(0, 0, 0), '', DateToStr(Договор_Дата, 'DD'))
  if (Договор_Дата = Date(0, 0, 0), '', DateToStr(Договор_Дата, 'MM'))
  if (Договор_Дата = Date(0, 0, 0), '', DateToStr(Договор_Дата, 'YYYY'))

  Номер Дата

  if (Период_С  = Date(0, 0, 0), '', DateToStr(Период_С,  'DD.MM.YYYY'))
  if (Период_По = Date(0, 0, 0), '', DateToStr(Период_По, 'DD.MM.YYYY'))

  Договор_Сумма Договор_Валюта

#ifdef KS_IN_NDE
  Цена_НДЕ    Цена_НДЕ
#else
  Цена_Валюта Цена_Валюта
#end

#ifdef KS_2_GR
!объект
  StrNN

#ifdef KS_IN_NDE
  Цена_НДЕ    Цена_НДЕ
#else
  Цена_Валюта Цена_Валюта
#end

  InsAfterAllSimbol(Объект_спец)
#end

  StrNN

#ifdef KS_IN_NDE
  Цена_НДЕ    Цена_НДЕ
#else
  Цена_Валюта Цена_Валюта
#end

  DoubleToStr(KolItog,  Kol_Pr)
  DoubleToStr(Sum1Itog, Cena_Pr)

!приложение
  StrNN
#ifdef KS_IN_NDE
  Цена_НДЕ    Цена_НДЕ
#else
  Цена_Валюта Цена_Валюта
#end

!Спецификация
    Спец_НПП
!    Спец_Поз_По_Смете
    Спец_Название
!    Спец_Номер_Расц
    Спец_Един_Изм
    DoubleToStr(Спец_Колич, Kol_Pr)
  #ifdef KS_IN_NDE
    DoubleToStr(Спец_Цена_НДЕ,  Cena_PrR)
    DoubleToStr(Спец_Стоим_НДЕ, Cena_PrR)
  #else
    DoubleToStr(Спец_Цена,  Cena_Pr)
    DoubleToStr(Спец_Стоим, Cena_Pr)
  #end

    DoubleToStr(KolItog, Kol_Pr)
    DoubleToStr(Sum1Itog,Cena_Pr)

#ifdef KS_2_GR
!итого по объекту
  StrNN
#ifdef KS_IN_NDE
  Цена_НДЕ    Цена_НДЕ
#else
  Цена_Валюта Цена_Валюта
#end
  Объект_спец
  DoubleToStr(KolItogObj, Kol_Pr)

#ifdef KS_IN_NDE
  DoubleToStr(Sum1ItogObj, Cena_PrR)
#else
  DoubleToStr(Sum1ItogObj, Cena_Pr)
#end

  DoubleToStr(KolItog, Kol_Pr)

#ifdef KS_IN_NDE
  DoubleToStr(Sum1Itog, Cena_PrR)
#else
  DoubleToStr(Sum1Itog, Cena_Pr)
#end

#end
!Итоги
  DoubleToStr(KolItog, Kol_Pr)

#ifdef KS_IN_NDE
  DoubleToStr(Sum1Itog, Cena_PrR)
#else
  DoubleToStr(Sum1Itog, Cena_Pr)
#end

  DoubleToStr(KolItogFull, Kol_Pr)

#ifdef KS_IN_NDE
  DoubleToStr(Sum1ItogFull, Cena_PrR)
#else
  DoubleToStr(Sum1ItogFull, Cena_Pr)
#end
!Подписи
  ФИО
.endfields
