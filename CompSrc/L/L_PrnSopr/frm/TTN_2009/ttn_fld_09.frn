!Поля в ТТН1
!Cena_Pr определяется в format.frn и ttn_init.frn
#ifdef _TTN_DOC_600_
#define _TTN_SKL_ORG_
#end
#ifdef _TTN_DOC_ROZN_600_
#define _TTN_SKL_ORG_
#end

#ifdef _TTN_SKL_ORG_
  #include 600_092.frn
#else
  #include TTN_092.frn
#end

#ifdef _IN_VAL_DOC_
  НакСимвол НакСимвол НакСимвол НакСимвол
#end

#ifndef _TTN_ALC_
  if (wRoundMass2 > 5, 'г.',
    if (wRoundMass2 > 2, 'кг.', 'т.'))
#end

Номер DateToStr(Дата,'DD mon YYYY г.')  strNN

#ifdef _TTN_SKL_ORG_
  if (Заказчик_Плательщик = ''
  , #OrgTTNBelAddr(Поставщик)
  , #OrgTTNBelAddr(Заказчик_Плательщик))

  if (Грузоотправитель = '',
    Поставщик + '(' + Склад + if (Склад_Адрес = '', '', ', ' + Склад_Адрес) + ')',
    #OrgTTNBelAddr(Грузоотправитель))

  if (Грузоотправитель_Головная_Орг_Наимен = '', '', '(' + Грузоотправитель_Головная_Орг_Наимен + ')')

  if (Грузополучатель = '',
    Поставщик + '(' + Склад_Пол + if (Склад_Пол_Адрес = '', '', ', ' + Склад_Пол_Адрес) + ')',
    #OrgTTNBelAddr(Грузополучатель))

  if (Грузополучатель_Головная_Орг_Наимен = '', '', '(' + Грузополучатель_Головная_Орг_Наимен + ')')

#else
  #OrgTTNBelAddr(Заказчик_Плательщик)

  #OrgTTNBelAddr(Грузоотправитель)
  if (Грузоотправитель_Головная_Орг_Наимен = '', '', '(' + Грузоотправитель_Головная_Орг_Наимен + ')')

  #OrgTTNBelAddr(Грузополучатель)
  if (Грузополучатель_Головная_Орг_Наимен = '', '', '(' + Грузополучатель_Головная_Орг_Наимен + ')')
#end

#ifdef _IN_VAL_DOC_
  НакСимвол НакСимвол НакСимвол НакСимвол
#end

#ifndef _TTN_ALC_
  if (wRoundMass2 > 5, 'г.',
    if (wRoundMass2 > 2, 'кг.', 'т.'))
#end

#ifdef _TTN_2009R_
     Номер_пп
#end
     Название_Доп
     ОтпЕд
     DoubleToStr(КолФ, Kol_Pr)
     sPriceMC
     sSummaMC

// ставка налога
#ifndef _TTN_2009R_
     if (ПризнакНДС = 0, '-', DoubleToStr(ставка_первого_налога, NDS_St))
#else
     if (СтавкаНДС7 = 0, '', DoubleToStr(СтавкаНДС7, '66.88'))
#end

// сумма НДС
#ifndef _TTN_2009R_
     if (ПризнакНДС = 0, '-', DoubleToStr(СтНДСНац, Cena_Pr))
#else
     if (СуммаНДС8 = 0, '', DoubleToStr(СуммаНДС8, Cena_PrR))
#end

// стоимость с НДС
#ifdef _TTN_2009R_
     DoubleToStr(ВсегоНДС9, Cena_PrR)
     if (СтавкаНП10 = 0, '', DoubleToStr(СтавкаНП10, '66.88'))
     DoubleToStr(СуммаНП11, Cena_PrR)
     DoubleToStr(ВсегоНДСНП12, Cena_PrR)
     DoubleToStr(РознЦена13, Cena_PrR)
#else
     DoubleToStr(СтНацСН, Cena_Pr)
#end

     if (KolGrM = '-', '', KolGrM)
     if (Признак_МЦ > 1,
       '',
   #ifdef _TTN_ALC_
     DoubleToStr(КолВес, Massa_Kg)
   #else
     MassaBrutto(КолВес, wRoundMass2)
   #end
     )
   // Примечание
   sPrim

#ifdef _TTN_2009R_
     Номер_пп
#end
     Название_Доп
     ОтпЕд
     DoubleToStr(КолФ, Kol_Pr)
     sPriceMC
     sSummaMC

// ставка налога
#ifndef _TTN_2009R_
     if (ПризнакНДС = 0, '-', DoubleToStr(ставка_первого_налога, NDS_St))
#else
     if (СтавкаНДС7 = 0, '', DoubleToStr(СтавкаНДС7, '66.88'))
#end

// сумма НДС
#ifndef _TTN_2009R_
     if (ПризнакНДС = 0, '-', DoubleToStr(СтНДСНац, Cena_Pr))
#else
     if (СуммаНДС8 = 0, '', DoubleToStr(СуммаНДС8, Cena_PrR))
#end

// стоимость с НДС
#ifdef _TTN_2009R_
     DoubleToStr(ВсегоНДС9, Cena_PrR)
     if (СтавкаНП10 = 0, '', DoubleToStr(СтавкаНП10, '66.88'))
     DoubleToStr(СуммаНП11, Cena_PrR)
     DoubleToStr(ВсегоНДСНП12, Cena_PrR)
     DoubleToStr(РознЦена13, Cena_PrR)
#else
     DoubleToStr(СтНацСН, Cena_Pr)
#end

     if (KolGrM = '-', '', KolGrM)
     if (Признак_МЦ > 1,
       '',
   #ifdef _TTN_ALC_
     DoubleToStr(КолВес, Massa_Kg)
   #else
     MassaBrutto(КолВес, wRoundMass2)
   #end
     )
   // Примечание
   sPrim

   с_грузом_след_док_доп с_грузом_след_док_доп2


#ifdef _TTN_2009R_
  ДрагМетНазв[1]   ДрагМетКол[1]   ДрагМетЕд[1]
  ДрагМетНазв[2]   ДрагМетКол[2]   ДрагМетЕд[2]
  ДрагМетНазв[3]   ДрагМетКол[3]   ДрагМетЕд[3]
  ДрагМетНазв[4]   ДрагМетКол[4]   ДрагМетЕд[4]
  ДрагМетНазв[5]   ДрагМетКол[5]   ДрагМетЕд[5]
  ДрагМетНазв[6]   ДрагМетКол[6]   ДрагМетЕд[6]
  ДрагМетНазв[7]   ДрагМетКол[7]   ДрагМетЕд[7]
  ДрагМетНазв[8]   ДрагМетКол[8]   ДрагМетЕд[8]
  ДрагМетНазв[9]   ДрагМетКол[9]   ДрагМетЕд[9]
  ДрагМетНазв[10]  ДрагМетКол[10]  ДрагМетЕд[10]
#end

#ifndef _TTN_2009R_
#ifdef _TTN_DOC_ROZN_
 DoubleToStr (if (isRozn911, if (bNalProd OR bFixGrMC, strАСтНацСБН, strАСтНацБН), strАСтНацСН), Cena_Pr)
#else
 DoubleToStr (if (bNalProd OR bFixGrMC, strАСтНацСБН, strАСтНацБН), Cena_Pr)
#end
 DoubleToStr(strАНДСНац,  Cena_Pr)
 DoubleToStr(strАСтНацСН, Cena_Pr)
 DoubleToStr(strАКолФ, RoundGrm)
#ifdef _TTN_ALC_
 DoubleToStr(strАКолВес, Massa_Kg)
#else
 MassaBrutto(strАКолВес, wRoundMass2)
#end

#ifdef _IN_VAL_DOC_
 doubleToString(Валюта_нрек, Round(strАНДСНац, GetPrecisionGroup('brForSopr_RVPrice')))
 doubleToString(Валюта_нрек, Round(strАСтНацСН,GetPrecisionGroup('brForSopr_RVPrice')))
#else
 doubleToString(0, Round(strАНДСНац, GetPrecisionGroup('brForSopr_Price')))
 doubleToString(0, Round(strАСтНацСН,GetPrecisionGroup('brForSopr_Price')))
#end
#else
  DoubleToStr(АСтоимость6, Cena_Pr)
  DoubleToStr(АСуммаНДС8, Cena_Pr)
  DoubleToStr(АВсегоНДС9, Cena_Pr)
  DoubleToStr(АСуммаНП11, Cena_Pr)
  DoubleToStr(АВсегоНДСНП12, Cena_Pr)
  DoubleToStr(strАКолФ, RoundGrm)
  MassaBrutto(strАКолВес, wRoundMass2)
  doubleToString(0, Round(АСуммаНДС8, GetPrecisionGroup('brForSopr_Price')))
  doubleToString(0, Round(АВсегоНДСНП12,GetPrecisionGroup('brForSopr_Price')))
#end

#ifdef _TTN_ALC_
  if (strАКолВес = 0, '', MassaInWords (strАКолВес, if (wRoundMass2 > 3, wRoundMass2, 3)))
#else
  if (strАКолВес = 0, '', MassaInWords (strАКолВес, wRoundMass2))
#end

  DoubleToStr(Round(strАКолФ, KolZn), strDr)

  отпуск_разрешил_должность  + ' ' + отпуск_разрешил
  if (ФИОДовер = '',
    if (Экспедитор = '', '', 'экспедитор ' + Экспедитор),
    ДовЛицоДолжность + if (ДовЛицоДолжность <> '','   ','') + ФИОДовер)
  сдал_отправитель_должность + ' ' + сдал_отправитель
  НомерДовер + ' ' + if (ДатаДовер <> date(0,0,0), DateToStr(ДатаДовер,'"DD" mon YYYY г.'),' ')
  if ((Trim(ФИОДовер) <> '') AND (ДатаДовер <> Date(0,0,0) OR Trim(НомерДовер) <> ''),
    if (Trim(ОргДовер) <> '', ОргДовер, Грузополучатель), '')

#ifndef _TTN_2009R_
#ifdef _TTN_DOC_ROZN_
 DoubleToStr (if (isRozn911, if (bNalProd OR bFixGrMC, strАСтНацСБН, strАСтНацБН), strАСтНацСН), Cena_Pr)
#else
 DoubleToStr (if (bNalProd OR bFixGrMC, strАСтНацСБН, strАСтНацБН), Cena_Pr)
#end
 DoubleToStr(strАНДСНац,  Cena_Pr)
 DoubleToStr(strАСтНацСН, Cena_Pr)
 DoubleToStr(strАКолФ, RoundGrm)
#ifdef _TTN_ALC_
 DoubleToStr(strАКолВес, Massa_Kg)
#else
 MassaBrutto(strАКолВес, wRoundMass2)
#end

#ifdef _IN_VAL_DOC_
 doubleToString(Валюта_нрек, Round(strАНДСНац, GetPrecisionGroup('brForSopr_RVPrice')))
 doubleToString(Валюта_нрек, Round(strАСтНацСН,GetPrecisionGroup('brForSopr_RVPrice')))
#else
 doubleToString(0, Round(strАНДСНац, GetPrecisionGroup('brForSopr_Price')))
 doubleToString(0, Round(strАСтНацСН,GetPrecisionGroup('brForSopr_Price')))
#end
#else
  DoubleToStr(АСтоимость6, Cena_Pr)
  DoubleToStr(АСуммаНДС8, Cena_Pr)
  DoubleToStr(АВсегоНДС9, Cena_Pr)
  DoubleToStr(АСуммаНП11, Cena_Pr)
  DoubleToStr(АВсегоНДСНП12, Cena_Pr)
  DoubleToStr(strАКолФ, RoundGrm)
  MassaBrutto(strАКолВес, wRoundMass2)
  doubleToString(0, Round(АСуммаНДС8, GetPrecisionGroup('brForSopr_Price')))
  doubleToString(0, Round(АВсегоНДСНП12,GetPrecisionGroup('brForSopr_Price')))
#end

#ifdef _TTN_ALC_
  if (strАКолВес = 0, '', MassaInWords (strАКолВес, if (wRoundMass2 > 3, wRoundMass2, 3)))
#else
  if (strАКолВес = 0, '', MassaInWords (strАКолВес, wRoundMass2))
#end

  DoubleToStr(Round(strАКолФ, KolZn), strDr)

  отпуск_разрешил_должность  + ' ' + отпуск_разрешил
  if (ФИОДовер = '',
    if (Экспедитор = '', '', 'экспедитор ' + Экспедитор),
    ДовЛицоДолжность + if (ДовЛицоДолжность <> '','   ','') + ФИОДовер)
  сдал_отправитель_должность + ' ' + сдал_отправитель
  НомерДовер + ' ' + if (ДатаДовер <> date(0,0,0), DateToStr(ДатаДовер,'"DD" mon YYYY г.'),' ')
  if ((Trim(ФИОДовер) <> '') AND (ДатаДовер <> Date(0,0,0) OR Trim(НомерДовер) <> ''),
    if (Trim(ОргДовер) <> '', ОргДовер, Грузополучатель), '')

#ifndef _TTN_2009R_
#ifdef _TTN_DOC_ROZN_
  DoubleToStr (if (isRozn911, if (bNalProd OR bFixGrMC, АСтНацСБН, АСтНацБН), АСтНацСН), Cena_Pr)
#else
  DoubleToStr (if (bNalProd OR bFixGrMC, АСтНацСБН, АСтНацБН), Cena_Pr)
#end
  DoubleToStr(АНДСНац,  Cena_Pr)
  DoubleToStr(АСтНацСН, Cena_Pr)
 if ((KolGrM = '') AND (sumKolf = 0), '', DoubleToStr(sumKolf, RoundGrm))
#ifdef _TTN_ALC_
  DoubleToStr(sumves, Massa_Kg)
#else
  MassaBrutto(sumves, wRoundMass2)
#end
#else
  DoubleToStr(АСтоимость6, Cena_Pr)
  DoubleToStr(АСуммаНДС8, Cena_Pr)
  DoubleToStr(АВсегоНДС9, Cena_Pr)
  DoubleToStr(АСуммаНП11, Cena_Pr)
  DoubleToStr(АВсегоНДСНП12, Cena_Pr)
  DoubleToStr(sumkolf,RoundGrm)
  MassaBrutto(sumves, wRoundMass2)
#end

  DoubleToStr(количество_ездок, '666')

!------------------------------------------------------------------------------
!вывод данных с оборотной страницы на 1-ю по настройке
#ifdef _IN_VAL_DOC_
  doubleToString(Валюта_нрек, Round(АНДСНац, GetPrecisionGroup('brForSopr_RVPrice')))
  doubleToString(Валюта_нрек, Round(АСтНацСН,GetPrecisionGroup('brForSopr_RVPrice')))
#else
  doubleToString(0, Round(АНДСНац, GetPrecisionGroup('brForSopr_Price')))
  doubleToString(0, Round(АСтНацСН,GetPrecisionGroup('brForSopr_Price')))
#end

#ifdef _TTN_ALC_
  if (sumves = 0, '', MassaInWords (sumves, if (wRoundMass2 > 3, wRoundMass2, 3)))
#else
  if (sumves = 0, '', MassaInWords (sumves, wRoundMass2))
#end

  DoubleToStr(Round(sumkolf, KolZn), strDr)

  отпуск_разрешил_должность  + ' ' + отпуск_разрешил
  if (ФИОДовер = '',
    if (Экспедитор = '', '', 'экспедитор ' + Экспедитор),
    ДовЛицоДолжность + if (ДовЛицоДолжность <> '','   ','') + ФИОДовер)
  сдал_отправитель_должность + ' ' + сдал_отправитель
  НомерДовер + ' ' + if (ДатаДовер <> date(0,0,0), DateToStr(ДатаДовер,'"DD" mon YYYY г.'),' ')

  if ((Trim(ФИОДовер) <> '') AND (ДатаДовер <> Date(0,0,0) OR Trim(НомерДовер) <> ''),
    if (Trim(ОргДовер) <> '', ОргДовер, Грузополучатель), '')
!------------------------------------------------------------------------------

#ifdef _TTN_ALC_
  Номер  DateToStr(Дата,'DD mon YYYY г.')  strNN
  Название2 удост_госгигиен_регистрации2 таможенные_разрешения2
  акцизные_марки1 акцизные_марки2 акцизные_марки3 акцизные_марки4 акцизные_марки5
  акцизные_марки6 акцизные_марки7 акцизные_марки8 акцизные_марки9 акцизные_марки10
#end

#ifdef _TTN_SKL_ORG_
  #include 600_092.frn
#else
  #include TTN_092.frn
#end

#ifdef _IN_VAL_DOC_
  НакСимвол НакСимвол НакСимвол НакСимвол
#end

#ifndef _TTN_ALC_
  if (wRoundMass2 > 5, 'г.',
    if (wRoundMass2 > 2, 'кг.', 'т.'))
#end

  Номер  DateToStr(Дата,'DD mon YYYY г.')  strNN

#ifndef _TTN_2009R_
#ifdef _TTN_DOC_ROZN_
  DoubleToStr (if (isRozn911, if (bNalProd OR bFixGrMC, АСтНацСБН, АСтНацБН), АСтНацСН), Cena_Pr)
#else
  DoubleToStr (if (bNalProd OR bFixGrMC, АСтНацСБН, АСтНацБН), Cena_Pr)
#end
  DoubleToStr(АНДСНац,  Cena_Pr)
  DoubleToStr(АСтНацСН, Cena_Pr)
  if ((KolGrM = '') AND (sumKolf = 0), '', DoubleToStr(sumKolf, RoundGrm))
#ifdef _TTN_ALC_
  DoubleToStr(sumves, Massa_Kg)
#else
  MassaBrutto(sumves, wRoundMass2)
#end

  DoubleToStr(количество_ездок, '666')

#ifdef _IN_VAL_DOC_
  doubleToString(Валюта_нрек, Round(АНДСНац, GetPrecisionGroup('brForSopr_RVPrice')))
  doubleToString(Валюта_нрек, Round(АСтНацСН,GetPrecisionGroup('brForSopr_RVPrice')))
#else
  doubleToString(0, Round(АНДСНац, GetPrecisionGroup('brForSopr_Price')))
  doubleToString(0, Round(АСтНацСН,GetPrecisionGroup('brForSopr_Price')))
#end

#ifdef _TTN_ALC_
  if (sumves = 0, '', MassaInWords (sumves, if (wRoundMass2 > 3, wRoundMass2, 3)))
#else
  if (sumves = 0, '', MassaInWords (sumves, wRoundMass2))
#end

  DoubleToStr(Round(sumkolf, KolZn), strDr)

  отпуск_разрешил_должность  + ' ' + отпуск_разрешил
  if (ФИОДовер = '',
    if (Экспедитор = '', '', 'экспедитор ' + Экспедитор),
    ДовЛицоДолжность + if (ДовЛицоДолжность <> '','   ','') + ФИОДовер)
  сдал_отправитель_должность + ' ' + сдал_отправитель
  НомерДовер + ' ' + if (ДатаДовер <> date(0,0,0), DateToStr(ДатаДовер,'"DD" mon YYYY г.'),' ')
  if ((Trim(ФИОДовер) <> '') AND (ДатаДовер <> Date(0,0,0) OR Trim(НомерДовер) <> ''),
    if (Trim(ОргДовер) <> '', ОргДовер, Грузополучатель), '')

#ifdef _IN_VAL_DOC_
  doubleToString(Валюта_нрек, Round(АНДСНац, GetPrecisionGroup('brForSopr_RVPrice')))
  doubleToString(Валюта_нрек, Round(АСтНацСН,GetPrecisionGroup('brForSopr_RVPrice')))
#else
  doubleToString(0, Round(АНДСНац, GetPrecisionGroup('brForSopr_Price')))
  doubleToString(0, Round(АСтНацСН,GetPrecisionGroup('brForSopr_Price')))
#end
#else
  DoubleToStr(АСтоимость6, Cena_Pr)
  DoubleToStr(АСуммаНДС8, Cena_Pr)
  DoubleToStr(АВсегоНДС9, Cena_Pr)
  DoubleToStr(АСуммаНП11, Cena_Pr)
  DoubleToStr(АВсегоНДСНП12, Cena_Pr)
  DoubleToStr(sumkolf,RoundGrm)
  MassaBrutto(sumves, wRoundMass2)
  DoubleToStr(количество_ездок, '666')
  doubleToString(0, Round(АСуммаНДС8, GetPrecisionGroup('brForSopr_Price')))
  doubleToString(0, Round(АВсегоНДСНП12, GetPrecisionGroup('brForSopr_Price')))
#end

#ifdef _TTN_ALC_
  if (sumves = 0, '', MassaInWords (sumves, if (wRoundMass2 > 3, wRoundMass2, 3)))
#else
  if (sumves = 0, '', MassaInWords (sumves, wRoundMass2))
#end

  DoubleToStr(Round(sumkolf, KolZn), strDr)

  отпуск_разрешил_должность  + ' ' + отпуск_разрешил
  if (ФИОДовер = '',
    if (Экспедитор = '', '', 'экспедитор ' + Экспедитор),
    ДовЛицоДолжность + if (ДовЛицоДолжность <> '','   ','') + ФИОДовер)
  сдал_отправитель_должность + ' ' + сдал_отправитель
  НомерДовер + ' ' + if (ДатаДовер <> date(0,0,0), DateToStr(ДатаДовер,'"DD" mon YYYY г.'),' ')
  if ((Trim(ФИОДовер) <> '') AND (ДатаДовер <> Date(0,0,0) OR Trim(НомерДовер) <> ''),
    if (Trim(ОргДовер) <> '', ОргДовер, Грузополучатель), '')

транспортная_услуга_1
транспортная_услуга_2

ТТН_ПунктП_Орг_Имя
ТТН_ПунктП_Метод
if (ТТН_ПунктП_Дата_С    = Date(0,0,0)          , '', DateToStr(ТТН_ПунктП_Дата_С,'DD/MM/YY'))
if (ТТН_ПунктП_Время_С   = String(Time(0,0,0,0)), '', ТТН_ПунктП_Время_С)
if (ТТН_ПунктП_Дата_По   = Date(0,0,0)          , '', DateToStr(ТТН_ПунктП_Дата_По,'DD/MM/YY'))
if (ТТН_ПунктП_Время_По  = String(Time(0,0,0,0)), '', ТТН_ПунктП_Время_По)
if (ТТН_ПунктП_Доп_Время = String(Time(0,0,0,0)), '', ТТН_ПунктП_Доп_Время)
ТТН_ПунктП_Доп_Имя
ТТН_ПунктР_Орг_Имя

 if (bDocEveryStr, '', все_документы1)
 if (bDocEveryStr, '', все_документы2)
 if (bDocEveryStr, '', все_документы3)
 if (bDocEveryStr, '', все_документы4)
 if (bDocEveryStr, '', все_документы5)

 if (bDocEveryStr, '',
   if (все_сертификаты1 = '', '', ' Сертификаты: ' + все_сертификаты1))

 if (bDocEveryStr, '', все_сертификаты2)
 if (bDocEveryStr, '', все_сертификаты3)
 if (bDocEveryStr, '', все_сертификаты4)
 if (bDocEveryStr, '', все_сертификаты5)

 if (ПрейскурантЦен  = '', '', ' Прейскурант: ' + Trim(ПрейскурантЦен))

Спецсимвол
#ifdef _TTN_SKL_ORG_
#undef _TTN_SKL_ORG_
#end