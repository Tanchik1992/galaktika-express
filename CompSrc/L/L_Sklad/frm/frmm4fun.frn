  var KolLine001, KolLine002: word;

  KolLine001 := KolAddInStr (MC_Name, 24, true);
  KolLine002 := KolAddInStr (MC_Kod, 12, true);

  if ( KolLine002 > KolLine001 )
    sch := sch + KolLine002 + 1;
  else
    sch := sch + KolLine001 + 1;
