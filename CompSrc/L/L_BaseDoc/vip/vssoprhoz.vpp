cmHozOper:
{
    if IsModified
      if not UpDateTable
        Exit;

    if ( GetFirst FastFirstRow SoprHoz = tsOk )
      RunInterface('SoprHoz', 1, typsoprhoz, 0, SoprHoz.nRec)
    else
      Message('Хозоперации по документу отсутствуют', Information);

    // прочитать изменения сделанные в окне привязки ТХО
    ReReadRecord;
}